import React, { useContext } from "react";
import { Context } from "../Context/context";

const Home = () => {
  const { Auth } = useContext(Context);
  return (
    <div>
      Home
      
    </div>
  );
};

export default Home;
